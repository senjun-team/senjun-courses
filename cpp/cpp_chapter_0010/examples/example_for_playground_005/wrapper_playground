import std;

int main()
{
    // У нас в плэйграунде компилятору передается специальный
    // флаг -Werror, превращающий предупреждения компилятора
    // в ошибки.
    
    // С этим флагом такой код не соберется.
    
    // Вы можете открыть иерархию проекта (слева от кода 
    // иконка с директорией), перейти в файл CMakeLists.txt 
    // и убрать флаг -Werror. Тогда код скомпилируется, а
    // в переменную len_km сохранится число 6.

    int len_km = 6.8;

    std::println("{}", len_km);
}