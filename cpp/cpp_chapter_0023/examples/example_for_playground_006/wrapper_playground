import std;

int main()
{
    // Биты числа flags от младшего 0-го к старшему 4-му:
    // 1, 0, 1, 0, 1
    int flags = 0b10101;
    
    // (1 << 3) - это число 0b1000.
    // Вычисляем
    //   0b10101
    // | 0b01000
    flags |= (1 << 3);

    std::println("flags={:b}", flags);
}