import std;

#INJECT-b585472fa

int main()
{
    std::println("to_string(Rarity::COMMON)    = {}", to_string(Rarity::COMMON));
    std::println("to_string(Rarity::RARE)      = {}", to_string(Rarity::RARE));
    std::println("to_string(Rarity::LEGENDARY) = {}", to_string(Rarity::LEGENDARY));
}