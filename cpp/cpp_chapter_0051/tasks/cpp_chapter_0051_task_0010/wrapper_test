import TestHelpers;
import std;

int main()
{
    std::string user_input = hlp::remove_spaces(R"(#INJECT-b585472fa)");
    std::sort(user_input.begin(), user_input.end());
    return user_input == "23" ? 0 : 1;
}
