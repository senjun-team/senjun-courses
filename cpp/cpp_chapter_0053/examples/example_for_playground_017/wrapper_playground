import std;

template<class T>
T max(T a, T b)
{
    return a < b ? b : a;
}

int main()
{
    std::println("{}", max(5, 6));
    std::println("{}", max('z', 'a'));
    std::println("{}", max(1.01, 1.02));
}