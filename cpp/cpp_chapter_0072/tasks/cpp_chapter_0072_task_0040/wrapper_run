import std;

#INJECT-b585472fa

int main()
{
    const std::deque<std::size_t> numbers = {4, 1, 3, 5};
    const std::size_t n = 5;

    const std::size_t fact = get_missing(numbers, n);
    const std::size_t plan = 2;

    std::println("get_missing({}, {}) returns {}. {}",
                 numbers,
                 n,
                 fact,
                 plan == fact ? "OK" : "ERR");
}