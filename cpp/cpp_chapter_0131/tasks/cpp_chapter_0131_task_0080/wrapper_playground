import std;

#INJECT-b585472fa

int main()
{
    const std::string raw_ip = "172.16.254.1";
    const std::uint32_t ip_plan = 2886794753;
    const std::uint32_t ip_fact = convert_ip_v4(raw_ip);

    std::println("convert_ip_v4(\"{}\") returns {}. {}",
    raw_ip, ip_fact, ip_fact == ip_plan ? "OK" : "ERROR");
}