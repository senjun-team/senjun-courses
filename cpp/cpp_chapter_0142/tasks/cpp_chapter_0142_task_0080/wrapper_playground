import std;

#INJECT-b585472fa

int main()
{
    std::vector<int> stock_prices{8, 1, 3, 2, 4, 1};
    const int fact = max_profit(stock_prices);
    const int plan = 3;
    std::println("max_profit({}) returns {}. {}", stock_prices, fact, fact == plan ? "OK" : "ERROR");
}