import std;

#INJECT-b585472fa

int main()
{
    int prev = 503;
    int cur = 200;

    std::println("calling swap(prev, cur) where prev={} and cur={}...", prev, cur);
    swap(prev, cur);
    std::println("After the call: prev={} and cur={}", prev, cur);
}