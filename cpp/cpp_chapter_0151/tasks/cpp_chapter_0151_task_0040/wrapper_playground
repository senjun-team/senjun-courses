import std;

#INJECT-b585472fa

int main()
{
    double prev = -1.2;
    double cur = 4.5;

    std::println("calling swap_via_pointers(prev, cur) where prev={} and cur={}...", prev, cur);
    swap_via_pointers(prev, cur);
    std::println("After the call: prev={} and cur={}", prev, cur);
}