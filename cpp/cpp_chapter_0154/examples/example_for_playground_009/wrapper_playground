import std;

int main()
{
    int n = 5;
    int * arr = new int[n];

    for (int i = 0; i < n; ++i)
    {
        arr[i] = i * i;
        std::println("{}-th element. Value: {}", i, arr[i]);
    }

    delete[] arr;
}