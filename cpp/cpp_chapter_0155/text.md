# Глава 15.5 Указатели на указатели

## Указатели на указатели

В C++ нельзя заводить [ссылки на ссылки.](/courses/cpp/chapters/cpp_chapter_0142/#block-reference-collapsing) А вот указатели на указатели — можно.

Указатель — это переменная, которая хранит адрес другой переменной. А указатель на указатель, получается, хранит адрес не просто какой-то переменной, а указателя.

```cpp
bool b = true;           // Переменная

bool * ptr = &b;       // Указатель

bool ** pptr = &ptr; // Указатель на указатель

// Разыменовываем pptr и получаем
// адрес, хранящийся в ptr
*pptr;

// Получаем значение b
**pptr;
```


![Указатель на указатель](https://raw.githubusercontent.com/senjun-team/senjun-courses/cpp-chapter-15-5/illustrations/cpp/pointer_to_pointer.jpg) {.illustration}



Указатель на указатель называют двойным указателем. Могут быть и указатели с любым другим уровнем косвенности. Например, указатель на указатель на указатель `T *** p`. Но на практике они почти не встречаются.

Количество звездочек `*` в объявлении определяет уровень косвенности:

```cpp
int * a = nullptr;     // Указатель
int ** b = nullptr;    // Двойной указатель
int *** c = nullptr;   // Тройной
```

## Зачем нужны двойные указатели

Основных сценариев применения указателей на указатели всего два:
- Работа с динамическими двумерными массивами (матрицами).
- Изменение указателя внутри функции.

### Динамические двумерные массивы

### Изменение указателя внутри функции

## Функция main() с аргументами командной строки

Функция `main()` — это точка входа в программу. И до сих пор мы пользовались ее простой формой, не принимающей параметров:

```cpp
int main()
{
    // ...
}
```

Но как быть, если мы хотим передать программе аргументы командной строки? На помощь приходит параметризованная функция `main()`. Она принимает два параметра:

```cpp
int main(int argc, char ** argv);
```

Имена параметров могут быть любыми, но принятыми считаются `argc` (argument count) и `argv` (argument vector).
- `argc` — это количество переданных программе параметров с учетом имени самой программы. 
- `argv` — массив строк с параметрами. Первым в нем идет имя программы.

Допустим, бинарь нашей программы называется `copy`, и мы запустили его из консоли с опциями `/home/ubiq/conf.json /home/ubiq/conf_backup.json`:

```bash
./copy /home/ubiq/conf.json /home/ubiq/conf_backup.json
```



```cpp
int main(int argc, char ** argv)
{
    std::println("Argument count: {}", argc);

    std::println("Argument vector: {}", )
}


----------

## Резюме
