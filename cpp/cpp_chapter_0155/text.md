# Глава 15.5 Указатели на указатели

## Указатели на указатели

В C++ нельзя заводить [ссылки на ссылки.](/courses/cpp/chapters/cpp_chapter_0142/#block-reference-collapsing) А вот указатели на указатели — можно.

Указатель — это переменная, которая хранит адрес другой переменной. А указатель на указатель, получается, хранит адрес не просто какой-то переменной, а указателя.

```cpp
bool b = true;           // Переменная

bool * ptr = &b;       // Указатель

bool ** pptr = &ptr; // Указатель на указатель

// Разыменовываем pptr и получаем
// адрес, хранящийся в ptr
*pptr;

// Получаем значение b
**pptr;
```


![Указатель на указатель](https://raw.githubusercontent.com/senjun-team/senjun-courses/cpp-chapter-15-5/illustrations/cpp/pointer_to_pointer.jpg) {.illustration}



Указатель на указатель называют двойным указателем. Могут быть и указатели с любым другим уровнем косвенности. Например, указатель на указатель на указатель `T *** p`. Но на практике такие чудеса почти не встречаются.

Количество звездочек `*` в объявлении определяет уровень косвенности:

```cpp
int * a = nullptr;     // Указатель
int ** b = nullptr;    // Двойной указатель
int *** c = nullptr;   // Тройной
```

## Зачем нужны двойные указатели

Основных сценариев применения указателей на указатели всего два:
- Работа с динамическими двумерными массивами (матрицами).
- Изменение указателя внутри функции.

### Динамические двумерные массивы

### Изменение указателя внутри функции

## Функция main() с аргументами командной строки

Функция `main()` — это точка входа в программу. И до сих пор мы пользовались ее простой формой без параметров:

```cpp
int main()
{
    // ...
}
```

Но как быть, если мы хотим передать программе аргументы командной строки? На помощь приходит параметризованная форма `main()`:

```cpp
int main(int argc, char ** argv);
```

Имена двух параметров могут быть любыми, но принятыми считаются `argc` (argument count) и `argv` (argument vector).
- `argc` — это количество переданных программе аргументов с учетом имени самой программы. 
- `argv` — массив указателей на [сишные строки](/courses/cpp/chapters/cpp_chapter_0155/#block-c-string) с аргументами. Первым аргументом ОС подставляет указатель на строку с именем программы.

Допустим, бинарь называется `copy`, и мы запустили его из консоли, дополнительно передав опции:

```bash
./build/bin/copy --from conf.json --to conf_backup.json
```

Выведем содержимое `argc` и `argv`:

```cpp
int main(int argc, char ** argv)
{
    std::println("Arg count: {}", argc);

    for (int i = 0; i < argc; ++i)
        std::println("{}: {}", i, argv[i]);
}
```
```
Arg count: 5
0: ./build/bin/copy
1: --from
2: conf.json
3: --to
4: conf_backup.json
```

Аргументы разделяются пробелами. Если в самом аргументе есть пробел, то аргумент берут в кавычки.

```bash
./build/bin/copy --from conf.json --to "./useful data/conf_backup.json"
```
```
Arg count: 5
0: ./build/bin/copy
1: --from
2: conf.json
3: --to
4: data/conf_backup.json
```

Все аргументы попадают в `main()` в качестве сишных строк. Чтобы получить из них числа, есть функции:
- [std::stoi()](https://en.cppreference.com/w/cpp/string/basic_string/stol) принимает строку и возвращает `int`.
- [std::stod()](https://en.cppreference.com/w/cpp/string/basic_string/stof.html)


Обе функции кидают исключение `std::invalid_argument`, если преобразование невозможно. И `std::out_of_range`, если результирующее число выходит за пределы диапазона типа. Чтобы получить из строки число типа `long long`, `float`, `long double` или любого другого, используются функции с соответствующими суффиксами: `std::stoll()` и другие.  

----------

## Резюме
