from io import StringIO
import sys

old_stdout = sys.stdout
sys.stdout = mystdout = StringIO()

#INJECT-b585472fa

sys.stdout = old_stdout

output = mystdout.getvalue().translate(str.maketrans("", "", " \t\n\r"))

if output != "CA":
    print("print() in C().f() output isn't correct")
    exit(1)
