from io import StringIO
import sys

old_stdout = sys.stdout
sys.stdout = mystdout = StringIO()

#INJECT-b585472fa

sys.stdout = old_stdout

res = mystdout.getvalue()
if res != "{'a': 'a', 'b': 'b'}\n" and res != "{'b': 'b', 'a': 'a'}\n":
    print("print() output isn't correct")
    exit(1)

text = """
#INJECT-ESCAPED-b585472fa
"""

if text.find("locals()") == -1:
    print("There is no 'locals()' call in solution")
    exit(1)

