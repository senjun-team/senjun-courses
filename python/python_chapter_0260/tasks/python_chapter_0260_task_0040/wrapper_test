from io import StringIO
import sys

old_stdout = sys.stdout
sys.stdout = mystdout = StringIO()

#INJECT-b585472fa

sys.stdout = old_stdout

for f in ["a", "b", "d"]:
    if f not in globals():
        print(f"Function '{f}' doesn't exist")
        exit(1)

if "c" in globals():
    print(f"Function 'c' shouldn't exist")
    exit(1)