#![deny(unused_variables)]
#![deny(unused_mut)]
#![deny(unused_assignments)]
#![allow(dead_code)]

#INJECT-b585472fa

#[derive(Debug)]
enum TestResult {
    DeclarationRequired
}

fn remove_whitespace(text: &str) -> String {
    text.chars().filter(|c| !c.is_whitespace()).collect()
}

fn main() -> Result<(), TestResult> {

    let text: String = remove_whitespace(r#"#INJECT-b585472fa"#);

    if !text.contains("fncalc_season_number(season_name:&str)->u32") {
        println!("Нет объявления функции `fn calc_season_number(season_name: &str) -> u32`");
        return Err(TestResult::DeclarationRequired);
    }

    Ok(())
}
